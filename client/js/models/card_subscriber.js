if (typeof App === 'undefined') {
    App = {};
}
/**
 * Card Subscriber Model
 * @class CardSubscriber
 * @constructor
 * @extends Backbone.Model
 */
App.CardSubscriber = Backbone.Model.extend({
    storeName: 'card_subscriber'
});
