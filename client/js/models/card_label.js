if (typeof App === 'undefined') {
    App = {};
}
/**
 * Card label Model
 * @class CardLabel
 * @constructor
 * @extends Backbone.Model
 */
App.CardLabel = Backbone.Model.extend({
    storeName: 'card_label'
});
